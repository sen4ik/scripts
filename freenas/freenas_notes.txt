# view disks
sudo mount -v
sudo df
geom disk list
geom disk list da7
sudo camcontrol devlist
sudo gpart list
sudo diskinfo -v da8

# view partitions
sudo gpart show

# manual mount that actually worked for me. I had issues mounting NTFS, but mounting ext2 worked just fine.
sudo mount -t ext2fs /dev/da8p1 /mnt/test
